import { checkDatabaseConnection } from "../database/postgresDatabase.js";

export async function buildHealthStatus() {
  return {
    status: "ok",
    service: "theblackmoney-api",
    database: await checkDatabaseConnection(),
    timestamp: new Date().toISOString()
  };
}
